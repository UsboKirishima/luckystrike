/**
 * @name Restore
 * @description Restore git command
 * @param {string} x - git command structure
 * @param {string} o - git command options
*/

export const Restore = (x: string, o: string) => {};